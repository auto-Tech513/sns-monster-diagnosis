# 計装変更履歴

## 2026-05-30 P4 v2 リリース

- `shindan_share` に `channel` パラメータを追加（`x` / `line` / `webshare` / `copy`）。
- `monster_name_key` は本スプリントで削除。`monster_code` は `safeTrack` の自動付与に統一。
- `share_platform` はX経路のみ両建て継続。次々スプリント（2026-06中旬予定）で全削除を検討。
- `cheki_download` は変更なし。`app_language` / `monster_code` / `score_bucket` は `safeTrack` 自動付与で充足。
- GA4カスタムディメンション `channel`（イベントスコープ）は2026-05-30に登録済み。
